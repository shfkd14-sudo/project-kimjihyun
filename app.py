# BringBack 🎒

> 장소를 떠나기 전 챙겨야 할 물건을 체크하고, 두고 온 물건은 자동으로 기록·통계로 보여주는 Streamlit 웹앱.

## 1. 해결하는 문제 (한 문장)
**대학생이** 학교·카페·도서관을 옮겨 다니며 충전기·에어팟·학생증 등을 자주 두고 오는 문제를, **장소별 물건 체크리스트**로 떠나기 전 확인하고 두고 온 물건은 자동 기록·통계로 보여줘 **덜 잃어버리게** 한다.

## 2. 핵심 기능 (우선순위)
| 우선순위 | 기능 | 설명 |
|---|---|---|
| **1순위 (Must-Have)** | 장소별 물건 체크리스트 | 장소를 선택해 '나가기' → 등록 물건을 체크 → 체크 안 한(두고 가는) 물건 안내 |
| **2순위 (Should-Have)** | 장소 기반 알림 (1순위에 통합) | 브라우저 제약으로 GPS·푸시 불가 → '나가기' 시 장소별 체크리스트 동작으로 흡수 |
| **3순위 (Could-Have)** | 미챙김 통계 | 두고 온 물건을 자동 기록해 물건별 횟수를 막대그래프·최근 기록으로 표시 |

> 2순위 '장소 기반 알림'은 수동 체크아웃 모델에서 1순위에 흡수됨 — 가이드의 "핵심 1 + 보조 1 다이어트" 적용.
> GPS 자동 감지·푸시 알림은 브라우저 웹앱의 한계로 의도적으로 제외(수동 체크아웃이 대체).

## 3. 화면 구성
- 📍 **등록**: 장소·물건 추가/삭제
- 🚪 **나가기**: 장소 선택 → 체크리스트 → 미챙김 자동 기록
- 📊 **통계**: 물건별 미챙김 횟수(막대그래프) + 최근 기록

## 4. 실행 방법
```bash
pip install -r requirements.txt
streamlit run app.py
```
실행하면 브라우저가 자동으로 열립니다 (기본 주소 http://localhost:8501).

## 5. 데이터 저장 / 제약
- **단일 사용자, 로그인 없음, 개인정보 입력 없음.**
- 모든 데이터는 `data.json` 파일에 저장되어 새로고침·재시작에도 유지됩니다.
- 파일이 없는 첫 실행에서도 오류 없이 빈 상태로 시작합니다.

## 6. 테스트 시나리오
- **정상**: 도서관에 [노트북, 충전기, 학생증] 등록 → 나가기에서 **학생증만 체크** → "노트북, 충전기를 두고 가셨어요" 안내 + 통계에 각 1회 반영
- **실패/경계**:
  - 빈 이름으로 추가 → "이름을 입력해주세요" 오류 (추가 안 됨)
  - 물건 0개인 장소에서 나가기 → "이 장소에 등록된 물건이 없습니다" 안내
  - 같은 장소/물건 중복 추가 → "이미 등록된…" 차단
  - `data.json` 없이 첫 실행 → 빈 상태 정상 시작, 첫 등록 시 자동 생성

## 7. 스크린샷

### 📍 등록 화면
![등록 화면](screenshots/01-register.png)
장소를 추가하고, 선택한 장소(예: 도서관)에 물건을 등록·삭제한다. 빈 이름·중복은 거부된다.

### 🚪 나가기 화면
![나가기 화면](screenshots/02-checkout.png)
떠나는 장소를 선택하면 등록된 물건이 미체크 체크리스트로 표시된다. '나가기 완료' 시 체크 안 한 물건이 기록된다.

### 📊 통계 화면
![통계 화면](screenshots/03-stats.png)
물건별 미챙김 횟수(막대그래프)와 최근 미챙김 기록을 보여준다. (예: 충전기 3회)

## 8. AI 사용 내역
> AI를 '대리기사'가 아닌 '조수'로 활용했다. 핵심 결정과 수정은 내가 주도했고, 무엇을 시키고 무엇을 판단했는지 아래에 정리한다.

### 어디에 AI를 썼나
- **요구사항 구체화**: AI와 한 번에 하나씩 질문하는 구조화 인터뷰(6라운드)를 진행했고, 핵심 결정은 **내가 직접 선택**했다 — ① GPS 자동 감지 대신 '수동 장소 떠나기 체크' 채택 ② MVP 범위를 1순위(체크리스트)+3순위(통계)로 한정 ③ 프레임워크는 Streamlit ④ 데이터 모델은 '장소별 독립 물건 목록'.
- **코드 구현**: 위에서 내가 확정한 명세대로 AI가 `app.py`·`storage.py` 초안을 작성했다.
- **검토**: AI가 계획을 설계·품질·보안 다관점으로 점검했고, 그 지적을 내가 확인해 반영했다.

### 받은 결과 → 내가 판단·수정한 것
- AI 초기 제안은 원 기획(GPS·푸시 알림)을 따랐으나, **브라우저 웹앱에서는 GPS·푸시가 불가능**함을 확인하고 내가 '수동 체크아웃' 방식으로 방향을 바꿨다.
- "기능 다 넣자"는 욕심 대신, 가이드의 '핵심1+보조1 다이어트' 원칙에 따라 **2순위(장소 알림)는 1순위에 흡수**시켜 범위를 줄였다.
- AI가 제안한 단순 파일 저장이 중간 실패 시 깨질 수 있다는 점을 보고 **원자적 저장(temp+os.replace)**으로 바꾸게 했다.

### 사용한 프롬프트 예시
1. "Streamlit/Gradio 기반 웹앱을 만들기 위해 모호성 0%의 구체적인 계획을 세워줘. GPS·푸시가 안 되는 브라우저 환경 기준으로."
2. "장소별 물건 체크리스트 + 미챙김 통계 MVP를, 빈입력·중복·빈목록·첫실행 4가지 경계를 방어하도록 구현하고 실제로 실행되는지 검증해줘."

## 9. 개선 기록
> "A에서 문제가 생겨 B로 바꿨다" — 완성도의 증거 (가이드: 개선 기록 = 점수)

1. **GPS 자동 알림 → 수동 '장소 떠나기' 체크**
   - A: 원 기획은 GPS로 장소 이탈을 감지해 알림을 띄우는 방식.
   - 문제: Streamlit/Gradio는 브라우저 웹앱이라 백그라운드 위치 추적·푸시 알림이 불가능.
   - B: 장소를 선택해 '나가기'를 누르면 체크리스트가 뜨는 수동 방식으로 변경 → 브라우저에서 100% 동작.

2. **단순 파일 저장 → 원자적 저장 (temp + os.replace)**
   - A: `json.dump`로 `data.json`에 바로 쓰기.
   - 문제: 쓰는 도중 중단되면 파일이 깨져 데이터 전체가 유실될 위험.
   - B: 임시 파일에 먼저 쓰고 `os.replace`로 교체해 안전하게 저장 (`storage.py`의 `save_data`).

3. **체크박스가 리셋되지 않던 문제 → session_state 카운터로 리셋**
   - A: '나가기 완료' 후에도 체크 상태가 남아 다음 사용 시 혼란.
   - 문제: Streamlit 위젯 key를 그냥 지우면 KeyError 발생.
   - B: `checkout_round` 카운터를 체크박스 key에 포함하고 완료 시 증가시켜, 새 미체크 상태로 안전하게 리셋 (`app.py`의 `render_checkout`).
